{
  "name": "aiostreams-tracker-bridge",
  "version": "1.1.3",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/server.ts",
    "dev": "node --watch --env-file-if-exists=.env src/server.ts",
    "test": "node --test test/*.test.ts",
    "check": "node scripts/check.mjs"
  },
  "description": "SIMKL, PublicMetaDB and MDBList watch-state addon for AIOStreams Jellyfin",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/docloulou/AIOSync.git"
  }
}
